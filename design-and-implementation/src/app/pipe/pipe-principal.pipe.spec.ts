import { PipePrincipalPipe } from './pipe-principal.pipe';

describe('PipePrincipalPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePrincipalPipe();
    expect(pipe).toBeTruthy();
  });
});
